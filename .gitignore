# .gitignore 내용

# 1. 보안 파일 (절대 업로드 금지!)
.env
.env.local

# 2. 파이썬 관련 쓰레기 파일들
__pycache__/
*.pyc

# 3. 가상환경 폴더 (용량 큼)
.venv/
venv/
env/

# 4. 맥(Mac) 시스템 파일
.DS_Store

# 5. 로컬 데이터베이스 (필요하다면)
news_curator.db